# Implement queue (actionable feedback only)

Cursor: after ingest, append items below. Ship these into `~/visit-taniti` (and sync monorepo if needed), then `git push` once Vercel is linked.

## Pending

_None yet — run `./ingest.sh` after phone drops land in `inbox/`._

## Done (already in prototype from draft guerrilla)

- [x] Lodging budget filters
- [x] Persistent Book Visit CTA + mobile menu
- [x] Deep-link lodging/attractions → book form
- [x] Nav label “Getting Around”
